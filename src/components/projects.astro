---
import { Image } from "astro:assets";
import g3 from "/public/img/Projectos/svg/g3.svg";
import ikarus from "/public/img/Projectos/svg/ikarus.svg";

const icon = [
  { name: "G3", img: g3 },
  { name: "Ikarus", img: ikarus },
];


---

<!-- Slot C -->
<div data-swapy-slot="c" class="lg:col-span-2 col-span-3">
  <!-- Item C -->
  <div
    data-swapy-item="c"
    class="text-center w-full h-52 backdrod-blur-lg bg-white/10 shadow-black dark:shadow-slate-200 shadow-sm rounded-lg p-4 flex-1"
  >
    <!-- PROYECTOS  -->
    <div transition:name=`img-proyecto`>
      <a href="/sections/proyectos" class="prose dark:prose-invert text-xl font-bold"> PROYECTO </a>

      <div class="flex justify-center items-center gap-12">
      {icon.map((project) => (
        <div class = "" >
          <Image src={project.img} alt={project.name} class="w-32 h-32"/>
        </div>
        ))}
      </div>
      

    </div>
  </div>
</div>
