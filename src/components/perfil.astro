---
import { Image } from 'astro:assets';
const { src, alt, width = "64", height = "64", borderClass = "border-black" } = Astro.props;
---

<Image 
  src={src} 
  alt={alt} 
  class={` w-${width}  h-${height} border ${borderClass} shadow-sm rounded-full object-cover`} 
  loading="eager" 
  />


<!-- w-64 h-64 border border-black shadow-sm rounded-full object-cover -->