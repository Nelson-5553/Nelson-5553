---
interface Props {
    Name: string;
    Ruta: string;
}
const {Name, Ruta} = Astro.props
---

<a href={Ruta} class="text-sm font-semibold leading-6 text-gray-900" data-astro-prefech="tap"
        >{Name}</a
      >